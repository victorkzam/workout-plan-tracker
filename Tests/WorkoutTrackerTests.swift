import XCTest
@testable import WorkoutTracker

// MARK: - WorkoutTracker Tests

final class WorkoutTrackerTests: XCTestCase {

    func testPlaceholder() {
        // Placeholder test to verify the test target builds and runs
        XCTAssertTrue(true, "Test target is configured correctly")
    }
}
